<h1> Showing all works in this page </h1>

<table>
  <thead>
    <tr>
      <th> Pending Work </th>
    </tr>
  </thead>
  <% @works.each do |work| %>
    <% if work.user == current_user %>
      <tbody>
            <td> <%= work.work%> </td>
            <td>
              <%= link_to "Show", todolist_path(work)%>
              <%= link_to "Edit", edit_todolist_path(work)%>
            </td>
            <td><%= button_to "Done", work, method: :delete, data: {confirm: "Are you sure?"} %></td>
      </tbody>
    <% end %>
  <% end%>
  </tbody>
</table>

<%= link_to "Create", new_todolist_path%>
